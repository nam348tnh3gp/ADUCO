name: PlatformIO CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        board:
          - uno
          - megaatmega2560

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install PlatformIO
        run: |
          python -m pip install --upgrade pip
          pip install platformio

      - name: Create src directory and copy files
        run: |
          mkdir -p src
          # Copy all source files
          cp *.h src/ 2>/dev/null || true
          cp *.c src/ 2>/dev/null || true
          cp *.cpp src/ 2>/dev/null || true
          cp *.hpp src/ 2>/dev/null || true
          # Convert .ino to .cpp (PlatformIO requirement)
          for f in *.ino; do
            [ -f "$f" ] && cp "$f" "src/${f%.ino}.cpp"
          done
          echo "=== Files in src/ ==="
          ls -la src/

      - name: Generate platformio.ini
        run: |
          cat > platformio.ini << 'EOF'
          [env:${{ matrix.board }}]
          platform = atmelavr
          board = ${{ matrix.board }}
          framework = arduino
          build_flags =
              -Ofast
              -funroll-loops
              -flto
              -Wall
              -Wno-unused-variable
              -Wno-unused-function
          upload_speed = 115200
          monitor_speed = 115200
          EOF

      - name: Build firmware
        run: pio run

      - name: Upload firmware artifacts
        uses: actions/upload-artifact@v4
        with:
          name: firmware-${{ matrix.board }}
          path: .pio/build/${{ matrix.board }}/firmware.hex
          if-no-files-found: ignore
